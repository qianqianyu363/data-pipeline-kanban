# 📊 需求下发登记全链路看板 — 设计文档

## 一、数据范围

| 数据源 | 范围 | 说明 |
|--------|------|------|
| 各项需求跟踪 | 需求下发→采集→审核→发布 | 26 条任务（03-08月），主数据源 |
| 预算与结算 | 1-12月费用数据 | 51 条结算，含 25 年对比 3 列 |
| 加工预审 | 4492 条审核记录 | 新/老供应商、价格审核、通过率 |
| 质量抽审 | 公章/报价抽审 | 按 类目→月份 展开 |
| 问题反馈 | 1000 条反馈 | 共识/未共识 + 已共识标签 |
| 采集产能 | 1-8月 | 计划/实际需求量、家数/条数、员工人数 |
| 需求下发登记.json | 对话登记台账 | Claude 对话写入，解析器合并进 tasks |

## 二、环节与指标（8 个流程 KPI 卡片）

1. 📋 **需求下发**：需求及时率、需求总条数、需求项数、初审问题数、初审通过率
2. 🔍 **需求校核**（前置校验）：初审问题数、初审问题率
3. 🔧 **数据采集**（核心执行）：当月采集完成率、采集完成率、采集下发数、采集完成数、无效数据、发布数据
4. ✅ **问题闭环**（过程纠偏）：采集反馈问题、共识量、共识率
5. 📊 **质量审核**（质量管控）：公章预审通过率、需求准确率
6. 🚀 **加工审核发布**（入库交付）：入库通过率、新/老供应商占比、价格审核通过率、发布数据、审核总量
7. 💰 **结算与成本**（预算管控）：总预算、累计执行、预算执行率

## 三、分析区 Tab

- **📈 月度趋势总览**：7 流程 ECharts 走势（独立于筛选，始终显示全部月份）
- **📋 采集执行分析**：
  - 📈 **采集进度**：按结算类型 / 需求来源的完成率对比
  - 📊 **采集产能**：实际家数 / 条数、人均产能、计划/实际需求量、产能人数预测（线性回归）
  - 💰 **费用执行**：已产生 + 预测费用（虚线框）+ 去年费用对比折线
  - 📋 **任务清单**：进度 + 闭环三态（是/否/跨月跟进）+ 约定完成时间

## 四、筛选逻辑

| 筛选维度 | 选项 | 联动范围 |
|---------|------|---------|
| 需求项 | 全部 / 市场价 / … | KPI 卡片、采集进度 |
| 结算类型 | 全部 / 价格采集 / 价格更新 / 全系列报价单 / … | KPI 卡片、采集进度 |
| 月份 | 全部 / 03-08月 | KPI 卡片、采集进度、质量抽审 |
| 月度趋势 | 独立于筛选，始终显示全部月份 | — |

## 五、数据流水线

```
看板数据录入模板-调整.xlsx ─┐
                           ├─ parse_template.py ─▶ data.json ──index.html──▶ GitHub Pages
需求下发登记.json ───────────┘   （对话登记台账：去重 + 合并 + 派生值自算）
```

更新流程：
1. 在 Excel 模板改数据（6 个 Sheet）
2. `python parse_template.py` → 生成 data.json（自动同步 index.html 内嵌 dataEmbed）
3. `git add` + `commit` + `push origin main`
4. 等待 1-2 分钟，GitHub Pages 自动更新

## 六、关键设计决策

| 决策 | 说明 |
|------|------|
| openpyxl 公式缓存丢失 | 解析器用 `data_only=False` + 内置公式求值器自算派生值（`eval_formula`/`cell_val`） |
| 对话登记台账 | `需求下发登记.json` 与模板合并，同 名称+月份 去重，模板优先 |
| dataEmbed 内嵌块 | 解析器生成 data.json 后自动同步，离线 fallback 保持最新 |
| 浏览器导入 | TPL_COL 0-indexed 与 Python COL_MAP 一一对应，字段语义一致 |
| 前端零改动原则 | 台账新增月份严格字符串匹配，null 字段显示"—"安全 |
| dueMonth MMDD | 约定采集完成时间填 MMDD 数值（730=7月30），解析器/前端都先判 MMDD 再判日期序列号 |

## 七、关键发现（2026-08-10 数据快照）

| 问题 | 数据 | 严重程度 |
|------|------|---------|
| 整体采集完成率 | 48.9%（96258/196761） | P0 |
| 发布及时率 | 68.0%（17/25） | P1 |
| 反馈共识率 | 27.8%（278/1000） | P1 |
| 无效数据 | 886 条 | P1 |
| 初审问题 | 4857 条（淄博1193最多） | P1 |

> ⚠️ 数据随模板更新而变化，以看板实时显示为准。

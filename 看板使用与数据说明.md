# 📊 需求下发登记全链路看板 — 使用与数据说明

## 一、看板结构

### 顶部：流程 KPI 卡片（8 环节，随筛选联动）

| 环节 | 核心指标 | 数据来源 |
|------|---------|---------|
| 📋 **需求下发** | 需求及时率、需求总条数、需求项数、初审问题数、初审通过率 | tasks |
| 🔍 **需求校核（前置校验）** | 初审问题数、初审问题率 | tasks.reviewIssues |
| 🔧 **数据采集（核心执行）** | 当月采集完成率、采集完成率、采集下发数、采集完成数、无效数据、发布数据 | tasks |
| ✅ **问题闭环（过程纠偏）** | 采集反馈问题、共识量、共识率 | issueFeedback |
| 📊 **质量审核（质量管控）** | 公章预审通过率、需求准确率 | qualityReview + tasks |
| 🚀 **加工审核发布（入库交付）** | 入库通过率、新/老供应商占比、价格审核通过率、发布数据、审核总量 | preReview + tasks |
| 💰 **结算与成本（预算管控）** | 总预算、累计执行、预算执行率 | settlements |

### 分析区 Tab

- **📈 月度趋势总览**：需求总条数 / 采集下发数 / 采集完成数 / 完成率 / 准确率 / 无效数据 / 预算执行率 逐月走势（独立于筛选，始终显示全部月份）
- **📋 采集执行分析**：4 个 Tab
  - **📈 采集进度**：按结算类型 / 需求来源的完成率对比
  - **📊 采集产能**：实际家数 / 条数、人均产能、计划/实际需求量、产能人数预测（线性回归）
  - **💰 费用执行**：已产生费用 + 预测费用（虚线框）+ 去年费用对比折线
  - **📋 任务清单**：每项任务进度 + 闭环三态（是 / 否 / 跨月跟进）+ 约定完成时间

---

## 二、数据文件结构

```
看板/
├── index.html                           ← 看板页面（全部代码，含 dataEmbed 内嵌离线数据）
├── data.json                            ← 解析器产物（tasks + settlements + 质量抽审 + 产能 + 配置）
├── 看板数据录入模板-调整.xlsx            ← 主数据源模板（6 个 Sheet）
├── 看板数据录入模板-调整_升级版.xlsx     ← 升级版源文件（用户手工维护，覆盖主文件）
├── parse_template.py                    ← Python 解析器（模板 → data.json，含公式求值/台账合并）
├── 需求下发登记.json                     ← 对话登记台账（Claude 对话写入，解析器合并进 tasks）
├── _smoke_test.js                       ← 前端冒烟测试（node _smoke_test.js data.json index.html）
├── .nojekyll                            ← 禁用 Jekyll（GitHub Pages 需要）
├── README.md                            ← 使用说明
└── CLAUDE.md                            ← 项目工作约定 / 会话恢复依据
```

---

## 三、模板 Excel 的 6 个 Sheet

| Sheet | 内容（2026-08-10 数据） |
|-------|---------|
| **各项需求跟踪** | 26 条任务（月份/需求项/结算类型/下发量/采集量/完成率/准确率/及时率/约定采集完成时间等） |
| **预算与结算** | 51 条结算（1-12月 × 价格采集/价格更新/全系列报价单/信息价采集/额外激励；含 25 年对比 3 列） |
| **质量抽审** | 公章抽审 + 报价抽审（按 类目→月份 展开） |
| **加工预审** | 4492 条预审（审核人/月份/新老供应商/通过率/价格审核结果） |
| **问题反馈** | 1000 条反馈（共识 278 / 未共识 722，含已共识问题标签） |
| **采集产能** | 1-8月（计划/实际需求量、采集家数/条数、员工人数） |

---

## 四、数据更新方式

### 方式一：改模板 + 跑解析器（推荐，持久化）
```bash
cd D:/AI实操/看板
# 1. 在 看板数据录入模板-调整.xlsx 中改数据（或更新 _升级版.xlsx 后覆盖主文件）
# 2. 生成 data.json（自动同步 index.html 内嵌 dataEmbed）：
python parse_template.py
# 3. 提交推送：
git add data.json index.html parse_template.py 看板数据录入模板-调整.xlsx CLAUDE.md
git commit -m "更新看板数据" && git push origin main
```

### 方式二：浏览器直接导入模板
1. 打开 https://qianqianyu363.github.io/data-pipeline-kanban/
2. 点击「导入」→ 选择 `看板数据录入模板-调整.xlsx` → 自动解析全部 6 个 Sheet，图表即时更新
3. 若需持久化，把更新的 data.json 提交到仓库

---

## 五、核心数据结论（2026-08-10 模板快照）

| 指标 | 数值 | 状态 |
|------|------|------|
| 整体采集完成率 | 48.9%（96258/196761） | 🔴 低于目标90% |
| 发布及时率 | 68.0%（17/25） | 🔴 8项未及时 |
| 预算执行率 | 23.1%（90.2万/390万） | 🟢 进度偏慢 |
| 预审通过率 | 99.8%（4492条） | 🟢 优秀 |
| 反馈共识率 | 27.8%（278/1000） | 🔴 严重偏低 |
| 无效数据 | 886条 | 🔴 需排查 |
| 初审问题 | 4857条（淄博1193最多） | 🔴 集中度高 |

> ⚠️ 数值随模板更新而变化，以上为生成 data.json 时的快照，以看板实时显示为准。

---

## 六、关键字段说明（升级版模板列号）

| 字段 | 含义 | 模板列 |
|------|------|--------|
| totalPoints | 需求总条数（下发数量，=下发材料×3） | Col 13 |
| itemCount | 需求项数（下发材料） | Col 14 |
| matQty | 材料数量（需求准确率分母） | Col 9 |
| reviewIssues | 预审问题数据 | Col 15 |
| accuracyRate | 需求准确率（=下发材料/材料数量） | Col 18 |
| collected | 实际采集完成量 | Col 22 |
| completionRate | 采集完成率（=采集量/下发量） | Col 24 |
| invalidData | 无效数据 | Col 25 |
| invalidRate | 无效数据占比（=无效/下发材料） | Col 26 |
| publishedData | 发布数据 | Col 27 |
| timelyRate | 需求发布及时率（0=延迟/1=及时） | Col 12 |
| dueMonth | 约定采集完成时间（MMDD，如 730=7月30） | Col 7 |

---

## 七、修复与改进记录

| 日期 | 修改 | 说明 |
|------|------|------|
| 2026-07-03 | 多Sheet导入 | 浏览器导入支持全部 Sheet 自动解析 |
| 2026-07-03 | 6环节重构 | 看板拆分为 Part1 核心指标 + Part2 详细指标 + Part3 趋势 |
| 2026-08-04 | 升级版模板 | 模板升级（约定完成时间/25年对比/价格审核/采集产能）+ 解析器公式求值 + 前端7项优化 |
| 2026-08-04 | UI指标去重 | 移除需求总量/未共识量/审核人明细等；质量抽审按 类目→月份 展开 |
| 2026-08-10 | 新版模板适配 | 结算 row4 起/25年3列、采集产能 9 列含需求量、dueMonth MMDD、问题反馈公式求值、产能需求量展示 |
